{
  "name": "tiktok-web-archive-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Small backend for resolving redirect links and storing public web archive snapshots.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
